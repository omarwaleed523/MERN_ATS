{
  "version": 2,
  "buildCommand": "npm install --omit=dev",
  "installCommand": "npm install --omit=dev",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
